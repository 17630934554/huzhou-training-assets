# Git ignore - 培训资料系统 V3
# 忽略：备份/缓存/部署临时包/QR 卡 PNG（生成物，不入版本）

# === 备份（不上传）===
_old_qr_backup/
*.bak
*.swp
*.tmp
*~

# === Python 缓存 ===
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/

# === 部署临时包（不存历史） ===
training_hosting.zip
*.zip

# === QR 卡 PNG（生成物，add_training.py 重新生成） ===
# 注意：hosting/qr/ 目录整体忽略（QR 卡是 build 产物，不是 source）
# 用 /qr/ 锚定到 hosting 根目录，避免误匹配 assets/qr/ 目录
/qr/
# 但 qr/ 里如果有重要参考图可单独加 !/qr/ref_xxx.png 例外

# 例外：assets/qr/ 目录是「飞书多维表格 record QR卡 字段引用」的 hosted 版本
# - 文件名用英文短名（lesson_1.png / manual.png / h1_report.png）避免 URL 编码
# - 推上 GitHub 后走 Cloudflare Pages 公开访问
# - 飞书多维表格 record「QR卡」字段值 = https://huzhou-training.pages.dev/assets/qr/xxx.png
!assets/qr/*.png
!assets/qr/.gitkeep

# === 系统文件 ===
.DS_Store
Thumbs.db
desktop.ini

# === IDE ===
.vscode/
.idea/
*.swp
*.swo

# === Git ===
.git/
.gitattributes

# === 私有凭证（如果大刘误提交） ===
*token*
*.env
.env
*.pem
*.key
